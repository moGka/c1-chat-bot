# Cloudflare Workers Configuration
# Replace with your actual Cloudflare Worker URL after deployment
REACT_APP_API_ENDPOINT=https://deepseek-chat-api.YOUR_SUBDOMAIN.workers.dev

# DeepSeek API Configuration (handled by Cloudflare Workers)
# These are configured in wrangler.toml and should not be exposed to the frontend
# DEEPSEEK_API_KEY=your_deepseek_api_key_here
# DEEPSEEK_API_BASE=https://api.deepseek.com/v1